{
  "name": "wms-config-generator",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run build && bun --watch server.ts",
    "build": "tsc --noEmit && bun build src/main.ts --outdir public/assets",
    "start": "bun server.ts",
    "test": "bun test"
  },
  "dependencies": {
    "fast-xml-parser": "^5.2.5"
  },
  "devDependencies": {
    "bun-types": "^1.0.0",
    "typescript": "^5.7.3"
  }
}
